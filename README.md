<h1>C++ Framework built on top of SDL3 for easy and fast gamedev prototyping</h1>
<p>Add the core folder in a project with SDL3, SDL3_image and SDL3_ttf dependencies set up and run <code>./build.sh {file.cpp} {output}</code> in the terminal to build it</p>
